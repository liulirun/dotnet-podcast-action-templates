# pass in params/input - tbd
# pass out params/input - tbd
# print env - tbd
# print inputs - tbd
on:
  workflow_call:
    inputs:
      hd_num:
        required: true
        type: string
      manual_param1:
        required: true
        type: string

jobs:
  build-mobile:
    runs-on: windows-latest

    steps:
      - name: Echo env
        shell: pwsh
        run: |
          echo "${{ inputs.manual_param1 }}"
          echo "${{ inputs.hd_num }}"
          echo "-----------------below are test-------------"          
          echo "${{ github.event.inputs.manual_param1 }}"
          